{
  "_description": "Model-aware compaction thresholds. Percentage (0-100) of context window that triggers auto-compaction.",
  "_note": "Keys are model IDs (just the ID part, not provider/id). The global value is used for any model not explicitly listed.",
  "_wildcard": "Use * for pattern matching, e.g., 'claude-*' matches all Claude models",
  "global": 70,
  "models": {
    "claude-opus-4-6": 80,
    "gpt-5.3*": 80,
    "gpt-5.2*": 75
  }
}
